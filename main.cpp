#include <iostream>
#include "include/Simulation/Clinic.h"

int main() {
    std::cout << "Clinic Patient Scheduling - project skeleton builds OK." << std::endl;

    // TODO (Member 4): once FileManager::LoadData exists, wire it up here:
    // Clinic clinic;
    // FileManager fm;
    // fm.LoadData("input.txt", clinic);
    // clinic.run();

    return 0;
}
